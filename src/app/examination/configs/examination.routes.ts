import {ExaminationComponent} from '../components/examination.component';
import {RouterModule} from '@angular/router';

export const ExaminationRoutes = RouterModule.forChild([
    {
        path: '',
        component: ExaminationComponent,
    }
]);
